name: Windows Zombie Mode (Edge Only)

on: workflow_dispatch

jobs:
  rdp-brutal:
    runs-on: windows-latest
    timeout-minutes: 360 # 6 Jam Max
    
    steps:
    - name: 1. Setup User & Firewall (Wajib)
      run: |
        net user runneradmin "PasswordSuper123!" 
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -name "fDenyTSConnections" -value 0
        Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -name "UserAuthentication" -value 1
        Write-Host ">>> Pintu RDP Terbuka." -ForegroundColor Green

    - name: 2. THE PURGE (Mode Kalem Tapi Mematikan)
      shell: powershell
      run: |
        $ErrorActionPreference = 'SilentlyContinue' # Kalo error, DIEM! Jangan nangis.
        
        Write-Host ">>> MEMULAI PEMBANTAIAN BLOATWARE..." -ForegroundColor Red
        
        # Power Plan
        powercfg -setactive 8c5e7fda-e8bf-4a96-9a85-a6e23a8c635c

        # Filter List - Hapus yang aman-aman aja biar ga crash
        $bloatware = Get-AppxPackage -AllUsers | Where-Object { 
            $_.Name -notmatch "Edge" -and 
            $_.Name -notmatch "SecHealth" -and 
            $_.Name -notmatch "UI.Xaml" -and
            $_.Name -notmatch "VCLibs" -and
            $_.Name -notmatch "System"
        }

        foreach ($app in $bloatware) {
            Write-Host "Menghapus: $($app.Name)"
            # Coba hapus, kalo gagal yaudah skip
            $app | Remove-AppxPackage -AllUsers -ErrorAction SilentlyContinue
        }

        # Paksa return true biar GHA ga stop
        Write-Host ">>> SELESAI. LANJUT BOS!" -ForegroundColor Green
        exit 0
        
    - name: 3. Pasang Cloudflare Tunnel (Jalan Pintas)
      env:
        CF_TOKEN: ${{ secrets.CF_TOKEN }}
      run: |
        Invoke-WebRequest -Uri https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe -OutFile cloudflared.exe
        Start-Process -FilePath ".\cloudflared.exe" -ArgumentList "tunnel --no-autoupdate run --token $env:CF_TOKEN" -NoNewWindow -PassThru
        Write-Host ">>> Tunnel Meluncur 🚀" -ForegroundColor Cyan

    - name: 4. Keep Alive (Looping Anti-AFK)
      run: |
        Write-Host ">>> SYSTEM READY. CONNECT SEKARANG!" -ForegroundColor Green
        while ($true) { Start-Sleep -Seconds 60; Write-Host "Masih hidup bang..." }
